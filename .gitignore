# Local sprite-review harness — not part of the shipped game
_spritelab.html
art-review/
assets/review/

# Local playwright install for running tests/ (no package.json on purpose)
node_modules/

# Claude Code dev-server config — local only
.claude/
